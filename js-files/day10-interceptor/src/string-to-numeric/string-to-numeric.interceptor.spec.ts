import { StringToNumericInterceptor } from './string-to-numeric.interceptor';

describe('StringToNumericInterceptor', () => {
  it('should be defined', () => {
    expect(new StringToNumericInterceptor()).toBeDefined();
  });
});
