import { TransformUserInputPipe } from './transform-user-input.pipe';

describe('TransformUserInputPipe', () => {
  it('should be defined', () => {
    expect(new TransformUserInputPipe()).toBeDefined();
  });
});
